import 'package:flutter/material.dart';
class CallScreeen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
          "Call"
      ),
    );
  }
}
